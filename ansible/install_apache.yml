---

- hosts: all
  become: true
  tasks:
  - name: update repository index
    apt: 
      update_cache: yes

  - name: install apache2 package
    apt:
      name: apache
      state: latest

  - name: add support php package for apache
    apt: 
      name: libapache2-mod-php
      state: latest
